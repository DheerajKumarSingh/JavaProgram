package practice;

public class MultiThreadingTest  {
	
	
	public MultiThreadingTest() {
		// TODO Auto-generated constructor stub
	}
	
	public static void main(String[] args) {
		// TODO Auto-generated method stub
		/*
		 * Thread t=new Thread(new MyRunnable()); Thread t2=new Thread(new
		 * MyRunnable()); Thread t3=new Thread(new MyRunnable()); Thread t4=new
		 * Thread(new MultiThreadingTest()); Thread t5=new Thread(new
		 * MultiThreadingTest()); t.start(); t2.start(); t3.start(); t4.start();
		 * t5.start();
		 */
}
	
}
